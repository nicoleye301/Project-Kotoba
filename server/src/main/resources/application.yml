server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://ep-square-sea-a5q5w4mx-pooler.us-east-2.aws.neon.tech/needs?user=needs_owner&password=QSrKY7U4jMWc&sslmode=require
    driver-class-name: org.postgresql.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
